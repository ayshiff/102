/*'use strict';
var expect = require('chai').expect
var isNumber = require('../src/isPure.js')
/** @test {isNumber} 
describe('isPure function test', () => {

    it('isPure(f) should return True  ', () => {
        let fonctionTest = (a) => {return a}
        let result = isPure(fonctionTest)
        supposedResult = true
        expect(result).to.equal(supposedResult)
    })

    it('isPure(f) should return True   ', () => {
        let fonctionTest = (a) => {undefined}
        let result = isPure(fonctionTest)
        supposedResult = false
        expect(result).to.equal(supposedResult)
    })

})*/